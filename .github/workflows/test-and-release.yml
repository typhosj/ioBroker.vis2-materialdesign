name: Test and Release

on:
  push:
    branches:
      - master
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
      - v[0-9]+.[0-9]+.[0-9]+-**
  pull_request:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-and-lint:
    if: contains(github.event.head_commit.message, '[skip ci]') == false
    runs-on: ubuntu-latest
    steps:
      - uses: ioBroker/testing-action-check@v2
        with:
          node-version: 24.x
          lint: true

  adapter-tests:
    needs: check-and-lint
    if: contains(github.event.head_commit.message, '[skip ci]') == false
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x, 24.x]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - run: npm ci
      - run: npm test

  # `widgets/` and `admin/assets/` are committed build output, and nothing else in this workflow
  # runs the build — so a source change released without a rebuild used to ship the OLD bundle
  # silently. Rebuild and require the result to match what is committed.
  build-is-current:
    needs: check-and-lint
    if: contains(github.event.head_commit.message, '[skip ci]') == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24.x
          cache: npm
      - run: npm ci
      - run: npm ci --prefix src-widgets-ts
      - run: npm run build
      - name: Committed bundles must match the build
        run: git diff --exit-code -- widgets admin/assets src-widgets-ts/src/generated
      # In this job and not in adapter-tests: pr-gate compares the two node_modules trees against
      # each other, and only this job installs src-widgets-ts.
      - run: npm run test:pr-gate

  deploy:
    needs: [check-and-lint, adapter-tests, build-is-current]
    if: |
      contains(github.event.head_commit.message, '[skip ci]') == false &&
      github.event_name == 'push' &&
      startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: ioBroker/testing-action-deploy@v1
        with:
          node-version: 24.x
          github-token: ${{ secrets.GITHUB_TOKEN }}
