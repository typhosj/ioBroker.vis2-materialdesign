import { createButtonClass } from './MaterialDesignButtons';

export default createButtonClass({ id: 'tplVis2-materialdesign-Button-State', name: 'Button State', kind: 'state', layout: 'default', label: '&nbsp;State', icon: 'pencil' });
